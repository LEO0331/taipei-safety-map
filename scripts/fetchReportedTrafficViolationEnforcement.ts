import { mkdir,writeFile }from'node:fs/promises';const dir='data/raw/reported-traffic-violation-enforcement';await mkdir(dir,{recursive:true});const r=await fetch('https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=8deab6c0-005e-4306-89fd-bff881c771a9');if(!r.ok)throw new Error(String(r.status));await writeFile(`${dir}/records.csv`,new Uint8Array(await r.arrayBuffer()));
