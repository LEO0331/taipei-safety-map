import { useMemo,useState } from 'react';import type { EmergencyOperationsCenterActivation,Language } from './types';
const t=(language:Language,zh:string,en:string)=>language==='zh'?zh:en;const levelLabel=(language:Language,value:string)=>value==='level_2_or_higher'?t(language,'二級以上','Level 2 or higher'):value==='enhanced_level_3'?t(language,'強化三級','Enhanced level 3'):value;
export default function EmergencyOperationsCenterActivations({records,language}:{records:EmergencyOperationsCenterActivation[];language:Language}){const[year,setYear]=useState('all'),[level,setLevel]=useState('all'),[query,setQuery]=useState('');const rows=useMemo(()=>records.filter(record=>(year==='all'||String(record.year)===year)&&(level==='all'||record.activationLevel===level)&&(!query||[record.disasterName,record.disasterReportRaw,record.yearRaw,record.sourceResourceName].join(' ').toLowerCase().includes(query.toLowerCase()))),[records,year,level,query]);const durations=rows.map(record=>record.activationDurationHours).filter((value):value is number=>value!==null).sort((a,b)=>a-b);return <main className="overview tobacco-control"><section className="hero"><p className="eyebrow">{t(language,'災害應變 · 歷史紀錄','Disaster Response · Historical Records')}</p><h2>{t(language,'災害應變中心開設歷史','Emergency Operations Center Activation History')}</h2><p className="notice">{t(language,'開設等級與期間為當時行政安排，不直接代表災害嚴重度、損失或個人風險。','Activation level and duration reflect administrative arrangements, not disaster severity, losses, or individual risk.')}</p></section><section className="filter-panel health-filters"><label>{t(language,'年度','Year')}<select value={year} onChange={event=>setYear(event.target.value)}><option value="all">{t(language,'全部','All')}</option>{[...new Set(records.map(record=>record.year).filter(Boolean))].sort().map(value=><option key={value} value={value!}>{value}</option>)}</select></label><label>{t(language,'開設等級','Activation level')}<select value={level} onChange={event=>setLevel(event.target.value)}><option value="all">{t(language,'全部','All')}</option><option value="level_2_or_higher">{levelLabel(language,'level_2_or_higher')}</option><option value="enhanced_level_3">{levelLabel(language,'enhanced_level_3')}</option></select></label><label className="search-field">{t(language,'搜尋','Search')}<input value={query} onChange={event=>setQuery(event.target.value)} placeholder={t(language,'災害名稱、災情或來源','Disaster, report, or source')}/></label></section><section className="summary-grid">{[[t(language,'紀錄數','Records'),rows.length],[t(language,'二級以上','Level 2+'),rows.filter(record=>record.activationLevel==='level_2_or_higher').length],[t(language,'強化三級','Enhanced level 3'),rows.filter(record=>record.activationLevel==='enhanced_level_3').length],[t(language,'有效期間資料','Valid durations'),durations.length],[t(language,'開設時數中位數','Median duration'),durations.length?durations[Math.floor(durations.length/2)].toFixed(1):'—'],[t(language,'最長開設時數','Longest duration'),durations.at(-1)?.toFixed(1)??'—']].map(([name,value])=><article className="metric" key={String(name)}><span>{name}</span><strong>{String(value)}</strong></article>)}</section><section className="panel table-wrap"><h3>{t(language,'歷史開設紀錄','Historical activation records')}</h3><table><thead><tr>{[t(language,'年度','Year'),t(language,'災害','Disaster'),t(language,'等級','Level'),t(language,'開設時間','Established'),t(language,'撤除時間','Deactivated'),t(language,'時數','Hours'),t(language,'災情紀錄','Report'),t(language,'來源','Source')].map(name=><th key={name}>{name}</th>)}</tr></thead><tbody>{rows.map(record=><tr key={record.id}><td>{record.year??'—'}</td><td>{record.disasterName||'—'}</td><td>{levelLabel(language,record.activationLevel)}</td><td>{record.establishedTimeRaw||'—'}</td><td>{record.deactivatedTimeRaw||'—'}</td><td>{record.activationDurationHours?.toFixed(1)??'—'}</td><td>{record.disasterReportRaw||'—'}</td><td>{record.sourceResourceName}</td></tr>)}</tbody></table></section><section className="panel"><p>{t(language,'此資料僅供歷史災害應變分析，不代表即時災情、目前開設狀態或官方緊急指示。','For historical analysis only; this does not represent real-time conditions, current activation status, or official emergency instructions.')}</p></section></main>}
