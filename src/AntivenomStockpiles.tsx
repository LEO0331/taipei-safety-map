import{useEffect,useState}from'react';import type{Language}from'./types';export default function AntivenomStockpiles({language}:{language:Language}){const[r,setR]=useState<any[]>([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/antivenom-stockpiles/approximate-district-records.json`).then(x=>x.json()).then(setR)},[]);return <main className="overview"><h2>{language==='zh'?'抗蛇毒血清儲備點':'Antivenom Stockpile Locations'}</h2><p className="notice">Antivenom stock may change. This is not real-time availability; suspected snakebite requires immediate medical attention.</p><input value={q}onChange={e=>setQ(e.target.value)}placeholder="Search facility, address, or material"/>{r.filter(x=>[x.facilityName,x.address,x.materialCategory].join(' ').includes(q)).map(x=><section className="panel"key={x.id}><h3>{x.facilityName}</h3><p>{x.materialCategory}<br/>{x.address}<br/><a href={`tel:${x.phone}`}>{x.phone}</a></p><a target="_blank"rel="noreferrer"href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(x.address)}`}>Directions</a></section>)}</main>}
