import{useMemo,useState}from'react';import type{Language,NarrowAlleyRecord}from'./types';const t=(language:Language,zh:string,en:string)=>language==='zh'?zh:en;const zone=(language:Language,value:string)=>value==='red_zone'?t(language,'紅區','Red zone'):value==='yellow_zone'?t(language,'黃區','Yellow zone'):value;
export default function NarrowAlleyRegistry({records,language}:{records:NarrowAlleyRecord[];language:Language}){const[district,setDistrict]=useState('all'),[query,setQuery]=useState('');const rows=useMemo(()=>records.filter(record=>(district==='all'||record.districtName===district)&&(!query||[record.alleyName,record.districtName,record.responsibleFireStation,record.zoneCategory,record.note].join(' ').toLowerCase().includes(query.toLowerCase()))),[records,district,query]);return <main className="overview tobacco-control"><section className="hero"><p className="eyebrow">{t(language,'消防通行 · 列管清冊','Fire Access · Registry Records')}</p><h2>{t(language,'狹小巷道紅黃區清冊','Narrow Alley Red and Yellow Zone Registry')}</h2><p className="notice">{t(language,'分類不代表即時通行、火災風險或特定建物安全程度。','Classification does not represent real-time accessibility, fire risk, or building safety.')}</p></section><section className="filter-panel health-filters"><label>{t(language,'行政區','District')}<select value={district} onChange={event=>setDistrict(event.target.value)}><option value="all">{t(language,'全部','All')}</option>{[...new Set(records.map(record=>record.districtName).filter(Boolean))].sort().map(value=><option key={value}>{value}</option>)}</select></label><label className="search-field">{t(language,'搜尋','Search')}<input value={query} onChange={event=>setQuery(event.target.value)} placeholder={t(language,'巷道、行政區或消防分隊','Alley, district, or fire station')}/></label></section><section className="summary-grid">{[[t(language,'列管巷道','Listed alleys'),rows.length],[t(language,'行政區','Districts'),new Set(rows.map(record=>record.districtName)).size],[t(language,'紅區','Red zones'),rows.filter(record=>record.zoneCategory==='red_zone').length],[t(language,'黃區','Yellow zones'),rows.filter(record=>record.zoneCategory==='yellow_zone').length],[t(language,'消防分隊','Fire stations'),new Set(rows.map(record=>record.responsibleFireStation)).size],[t(language,'有效寬度資料','Usable widths'),rows.filter(record=>record.widthMeters!==null).length]].map(([name,value])=><article className="metric" key={String(name)}><span>{name}</span><strong>{String(value)}</strong></article>)}</section><section className="panel table-wrap"><table><thead><tr>{[t(language,'序號','ID'),t(language,'分類','Category'),t(language,'行政區','District'),t(language,'消防分隊','Fire station'),t(language,'巷道名稱','Alley name'),t(language,'寬度','Width'),t(language,'備註','Notes')].map(name=><th key={name}>{name}</th>)}</tr></thead><tbody>{rows.map(record=><tr key={record.id}><td>{record.sourceSequenceNumber}</td><td>{zone(language,record.zoneCategory)}</td><td>{record.districtName||'—'}</td><td>{record.responsibleFireStation}</td><td>{record.alleyName}</td><td>{record.widthRaw||'—'}</td><td>{record.note||'—'}</td></tr>)}</tbody></table></section></main>}
